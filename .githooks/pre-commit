#!/bin/sh
# Installed via `npm run prepare` (git config core.hooksPath .githooks).
# Blocks any commit that would leak plaintext personal data.
set -e
repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"
node scripts/privacy-guard.mjs
